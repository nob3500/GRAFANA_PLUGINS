import React from 'react';
import { DataFrame } from '@grafana/data';
interface EventsCanvasProps<T> {
    id: string;
    events: DataFrame[];
    renderEventMarker: (event: T) => React.ReactNode;
    mapEventToXYCoords: (event: T) => {
        x: number;
        y: number;
    } | undefined;
}
export declare function EventsCanvas<T>({ id, events, renderEventMarker, mapEventToXYCoords }: EventsCanvasProps<T>): JSX.Element | null;
export {};
