import { AreaProps, LineProps, PointProps } from './types';
export declare const getAreaConfig: (props: AreaProps) => {
    scale: string;
    fill: string | undefined;
};
export declare const getLineConfig: (props: LineProps) => {
    scale: string;
    stroke: string;
    width: number;
};
export declare const getPointConfig: (props: PointProps) => {
    scale: string;
    stroke: string;
    points: {
        show: boolean;
        size: number;
        stroke: string;
    };
};
