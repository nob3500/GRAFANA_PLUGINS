#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@grafana/toolkit/node_modules/@grafana/e2e-selectors/node_modules/typescript/bin/tsserver" "$@"
  ret=$?
else 
  node  "$basedir/../@grafana/toolkit/node_modules/@grafana/e2e-selectors/node_modules/typescript/bin/tsserver" "$@"
  ret=$?
fi
exit $ret
